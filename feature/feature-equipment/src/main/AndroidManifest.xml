<?xml version="1.0" encoding="utf-8"?>
<!--
  The photo capture of §7.6 lives in this module, so the provider that carries its files does too:
  the app module should not have to know where equipment photos are kept for the feature to work.
  ${applicationId} is substituted at merge, so the authority always matches the installed app.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
