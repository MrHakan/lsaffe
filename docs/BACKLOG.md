# BACKLOG

Deferred work. Nothing in this list is stubbed as fake UI in the app — a
feature is either implemented or absent and listed here.

- Macrobenchmark module measuring deck-view frame timing on a seeded 20-deck
  vessel (CI informational job).
- Biometric/PIN app lock (Settings toggle exists only when implemented).
- Automatic weekly SAF-folder backup with 8-generation retention.
- Remaining IMO symbol coverage to the full ~150 target of §10.3.
- Remaining equipment-type coverage to the full §9.2 list.
- Survival-craft schematic generalisation to all system types (fixed CO₂ bank,
  SCBA, fire pumps) — lifeboat is the reference implementation.
- Turkish plain-language summaries for all regulation cards.
- Per-item opt-in reminders per task definition (daily digest ships first).
- Compose UI test suite expansion (add-equipment flow, import conflict UI).
- Tablet-optimised two-pane layouts.
- Release-please / git-cliff automated changelog pipeline.
